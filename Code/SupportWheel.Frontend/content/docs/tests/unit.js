/**
 * @ngdoc overview
 * @name Unit Tests
 * @description
 *
 * # Unit Tests for the Front-end #
 *
 * ## Karma ##
 * 
 * To run the tests for this solution you need to have KARMA runner installed in your system.
 * It is recommended to install also the karma-cli as global, that way you can run it anywhere. To install:
 * 
 <pre>
    npm install -g karma-cli
 </pre>
 *
 * To run the tests just a single time, type:
 *
 * <pre>grunt karma:unit</pre>
 *
 * or if you want it to be running all time, type:
 * <pre> karma start</pre>
 *
 *
 * ## HTML Report ##
 *
 * The tests are part of documentation, as so when the docs are generated a version is created in HTML format.
 * To check this version, follow the link below:
 *
 * To update this documentation with the unit tests run the task below
 *
 * <pre>grunt karma:unitHtml</pre>
 *
 * {@link /docs/tests/unit/index.html See Unit Tests}
 *
 */
